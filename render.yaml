services:
  - type: web
    name: height-predictor
    runtime: node
    plan: free
    buildCommand: npm install && pip install -r requirements.txt
    startCommand: npm start
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
